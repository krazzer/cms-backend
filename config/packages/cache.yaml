framework:
    cache:
        pools:
            key_value_store:
                default_lifetime: 31536000 # One year
                adapters:
                    - cache.adapter.apcu
                    - cache.adapter.doctrine_dbal